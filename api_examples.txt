1)
region list using AWS EC2 CLI :
for region in aws ec2 describe-regions --output text | cut -f4;
  do 
     echo -e "\nListing Instances in region:'$region'...";
      aws ec2 describe-instances --region $region | jq '.Reservations[] | ( .Instances[] | {state: .State.Name, name: .KeyName, type: .InstanceType, key: .KeyName})' > all_instances ;
done

2)
Each AWS Service (eg Amazon EC2, Amazon S3) have their own set of API calls. Also, each Region is independent.
So in this case we can use that moudle :
https://pypi.org/project/aws-list-all/
And run the following 
aws-list-all show XXX
